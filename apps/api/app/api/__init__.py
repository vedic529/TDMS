"""HTTP routers.

One router per SRS page will live here, for example `timetable.py`,
`students.py`, `student_imports.py`, `trainers.py`, `courses.py`,
`qualification_units.py`, `users.py` and `activity_records.py`.

The route names are already used by `ApiTdmsClient` in the frontend
(`apps/web/src/services/api-tdms-client.ts`), so the contract is fixed before
the endpoints are written.
"""
